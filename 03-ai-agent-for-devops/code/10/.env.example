# Environment variables for the AI Logging Agent - Chapter 10

# Required: Your Google Gemini API key
GEMINI_API_KEY=your_api_key_here

# Optional: Model configuration
GEMINI_MODEL=gemini-2.5-flash
TEMPERATURE=0.1

# Optional: Log directory path
LOG_DIRECTORY=logs

# Optional: AWS Configuration (leave empty for placeholder mode)
# The agent works without AWS credentials - it will simulate actions
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_RDS_INSTANCE_ID=orders-db-prod

# Optional: Slack Configuration (leave empty for placeholder mode)
# Create a webhook at: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=
SLACK_CHANNEL=#devops-alerts
